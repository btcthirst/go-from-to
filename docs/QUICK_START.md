# Швидкий старт - Fyne UI

## ✅ Що досягнуто:

Додана повнофункціональна графічна інтерфейс (GUI) на Fyne з:

1. **Три діалоги для вибору файлів:**
   - Вибір файлу нарахувань (*.ods)
   - Вибір файлу виписок (*.xlsx)
   - Вибір місця для результатів (*.xlsx)

2. **Інтерактивні элементи:**
   - Відображення вибраних файлів
   - Кнопка обробки (активна якщо все вибрано)
   - Статус-бар з повідомленнями

## 🚀 Як запустити:

### Варіант 1 - Запуск з вихідного коду:
```bash
cd /home/min/git-workspace/go-from-to
go run ./cmd/app/main.go
```

### Варіант 2 - Запуск компільованого бінарника:
```bash
./bin/app
```

### Варіант 3 - Новоібльна компіляція:
```bash
go build -o bin/app ./cmd/app/main.go
```

## 📁 Змінені файли:

| Файл | Що змінилось |
|------|-------------|
| `go.mod` | Додана залежність `fyne.io/fyne/v2 v2.5.0` |
| `cmd/app/main.go` | Запуск Fyne UI замість консолі |
| `internal/app/app.go` | Додана функція `ProcessFiles(...)` |
| `internal/excel/writer/writer.go` | `WriteResults()` тепер приймає `outputFile` параметр |
| `internal/ui/ui.go` | **НОВИЙ** - Fyne UI менеджер |

## 🎯 Використання:

```
1. Запустити програму
2. Вибрати файл нарахувань
3. Вибрати файл виписок
4. Вибрати місце для результатів
5. Натиснути "Обробити файли"
6. Дочекатися повідомлення про успіх
```

## 💡 Особливості:

- ✅ Стандартні системні діалоги (Windows, macOS, Linux)
- ✅ Асинхронна обробка (не зависає UI)
- ✅ Статус-бар з прогресом
- ✅ Українські напис на інтерфейсі
- ✅ Валідація (обробка неможлива без всіх файлів)

## 📦 Залежності:

Всі залежності автоматично завантажуються при `go mod tidy`:
```bash
go mod tidy  # Завантажити всі залежності
```

---
**Статус:** ✅ Готово до використання
